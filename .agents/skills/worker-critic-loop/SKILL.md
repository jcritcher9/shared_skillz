---
name: worker-critic-loop
description: Drive a worker→critic implementation loop from a lightweight orchestrator chat: spin up an Opus 4.8 medium-effort worker to implement and an Opus 4.8 high-effort critic to grade, looping per phase of a multi-phase scoping document (or once for a single task) until each phase earns Grade A / A−. Use when the user wants to run an assigned implementation via a worker/critic loop, or runs /worker-critic-loop.
---

# Worker / critic loop

The normative instructions for this role live in the runtime-neutral
protocol directory, not in this file:

**`../../../agent_protocol/worker-critic-loop.md`**

Read that file now and follow it exactly.

This stub exists only because Codex discovers skills at
`.agents/skills/worker-critic-loop/SKILL.md`. It carries no instructions of its own
and must not be edited in place. Any change to this role belongs in
`agent_protocol/worker-critic-loop.md`.
