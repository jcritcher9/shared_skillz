---
name: create-scoping-doc
description: Interactively write a new scoping document that follows `implementation_notes_strategy.md`: phases, slices, pass grain, current-state and testing pointers, and the required commit and pass tables. Use when asked to design, write, or draft a scoping document, strategy doc, or implementation notes, or when the user runs /create-scoping-doc.
argument-hint: doc-name
---

# Create a scoping document

The normative instructions for this role live in the runtime-neutral
protocol directory, not in this file:

**`../../../agent_protocol/create-scoping-doc.md`**

Read that file now and follow it exactly. Enter worktrees per
`../../../agent_protocol/README.md` §9.

This stub exists only because Grok discovers skills at
`.grok/skills/<name>/SKILL.md`. Any change to this role belongs in
`agent_protocol/create-scoping-doc.md`.

See `../../../agent_protocol/README.md` for the protocol and the other roles.
