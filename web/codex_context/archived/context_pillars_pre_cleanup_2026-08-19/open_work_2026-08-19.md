> [!WARNING]
> **Historical, non-authoritative snapshot.**
> Captured from `web/codex_context/open_work.md` at baseline `a44572328` on 2026-08-19.
> That living file was retired. The web backlog was deleted, not migrated. `web/codex_context/current_state.md` is the only living context document. Read this snapshot only for optional history.

---

# EasyImports Web — Open Work

Last updated: 2026-08-19 (**Created-date review-display rem Grade A− ACCEPT
`41736afc0`** / pin **`4b2b64786`.** **`7794622fd`** is **B+** history.
**Created-date scoring rem Grade A− ACCEPT
`bab1297c3`.** **`ece89f549`** is **B+** history. **Review End early rem Grade A− ACCEPT
`86f059951`** / pin **`09e94f5cf`.** Land **`c32f5d690`** is **B**
history. Prior exact-first 4A+4B+4C Grade A− ACCEPT
`a13c754dc`** / pin **`b051e3c2c`.** rem10 **`6c755fa6f`** is **B+**
history. rem9 **`df2361e6d`** remains **B+** history. **Phase 5 Grade A−
ACCEPT `7be8bdb82`** / pin **`cce4cef6c`.** rem2 **`68f74e9dc`** / rem
**`e58c1d8c3`** are **B+** history; original **`4a63c11c2`** is **B**
history. **4C auto-disposition rem2 Grade A− ACCEPT `2e451ff11`** / pin
**`00b76fff7`.** **`21da0960c`** is **B+** history. Local acceptance
Phase **6** stays not authorized. Product-default
remains **FE-CM-4**. Prior **ARW closed / archived.** **ARW-5 Grade A
ACCEPT** **`20d502b4`**. this-track next **none** on mappings_2.
Product-default remains
**FE-CM-4**. Prior **RSF closed / archived.** **RSF-5 Grade A
ACCEPT** **`96caece2`**. Prior RSF this-track next **none**. List-import full-path this-track next **5B** (**5A Grade
A- ACCEPT** rem2 **`837f4d5f`**). Prior **OUT-H Grade A- ACCEPT** **`9fc3fa71`** /
pin **`6005fe28`**; this-track next **OUT-2**. Prior **OUT-1 rem
Grade A- ACCEPT** **`21967ca6`**. Prior **OUT-I2 rem2 Grade A-
ACCEPT** **`81bf3bce`**. Prior: **CRM-dupe first-batch hang archived**
**`59ab64bc`** — Phase **3 Grade A ACCEPT** **`53846172`** / pin
**`039d9c07`**;
hang-track next **none**. Prior: **2-EXPORT Grade A-
ACCEPT** **`f802668e`** / pin **`e01b6742`**. Prior: **CRM connection-removal
status-aware dependents 0–6 Grade A ACCEPT / archived** — Phase **6**
**`fad0d2d6`**; does **not** displace **FE-CM-4**. Prior: **CRM-dupe
first-batch hang 2-PKG Grade A ACCEPT** **`da02b11a`** / pin
**`7ce2981e`**. Prior **2-OAUTH**
Grade A- ACCEPT** **`ddd9a3b0`** / pin **`577da807`**, API **1.35.0**.
Prior **2-KNOWN Grade A- ACCEPT** **`7942c7c3`** / pin **`ac3f538a`**. Prior: **CRM
disconnected-connection removal Phase 1
Grade A- ACCEPT** rem7 **`885c3a87`**; rem6 **`11fc6f90`**; **CRM reclaim
Phase 2 rem `b707ec4a` Grade A- ACCEPT**; **DRC-1…4 closed / archived** —
**DRC-1** **`cce61414`**, **DRC-2** **`4820fa47`**, **DRC-3 Grade A-
ACCEPT** **`e0bf4529`** — review clarity
`completed_projects/crm_duplicate_review_clarity.md`; **DRUX-1…5 closed /
archived** under
`completed_projects/`; product-default next **FE-CM-4** unchanged; production
path reliability and storage E2E **7C** retained)

Single living backlog for outstanding EasyImports **web** work.

Companions: `current_state.md`, `handoff_doc.md`. Product authority for FE-CM
and MAP: `mappings_2/codex_context/open_work.md`. FE-CM-1/2/3 accepted; **FE-CM-4**
product-default next. CRM connection UX local-MVP + OAuth HTTPS-0…3 complete.

**DDR-0…5 closed / archived** rem **`c0d00cfb`** / acceptance **`0adbc526`**.
Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_duplicate_discovery_and_review_performance.md`.
Track next: **none**. Does **not** displace **FE-CM-4**.

---

## CRM duplicate review window integrity (ARW) (completed / archived)

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_duplicate_review_window_integrity_and_identity_cost.md`.

**Does not displace product-default next FE-CM-4.** **ARW-1…ARW-5
accepted.** **ARW-5 Grade A ACCEPT** **`20d502b4`**. Skip and
quarantine render below the comparison table with the same
`duplicate_choice` values. Confirm winner posts the displayed child.
GET never writes. Django still does not import `mappings_2`.

**Track next:** **none** (closed / archived). See
`mappings_2/codex_context/open_work.md`.

---

## CRM duplicate-group pipeline efficiency (GFC) (completed / archived)

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_duplicate_group_pipeline_efficiency.md`.

**Does not displace product-default next FE-CM-4.** Complements closed
DDR. **GFC-7B Grade A− ACCEPT** rem **`9b3e2424`** / pin **`7929eed7`**
(`690c98de` is **B+** history): bulk domain routing,
residual producers, `EvidencePolicy.version` **5**, public
`routing_domain_groups`, API **1.52.0**. **GFC-3 Grade A− ACCEPT** rem3
**`4709a803`** / pin **`e2f0b6ba`**: ceiling **128** remains in force.
**GFC-7A Grade A− ACCEPT** pin **`0fdf7e86`**. **GFC-4 Grade A ACCEPT** code
**`055a9dff`** / pin **`225b42e6`**: Django distinguishes
`building_recommendations` vs `building_approval_bundle` copy;
`complete` keeps the existing terminal sentence. **GFC-1 Grade A
ACCEPT** code **`6b191fdc`** / pin **`7d4a0b25`**: incremental member
index for `_compatible_partition`. **GFC-2 Grade A ACCEPT** code **`9d3fbd6c`** / pin **`749a4e66`**:
one `reconstruct_discovery` on CAS win; CAS-loss reconstructs
against the winner. **GFC-5 Grade A ACCEPT** code **`15d6b7f1`** / pin **`1f7b4f12`**:
indexed `build_group_revisions` lookups. No API/version change.
Django still does not import `mappings_2`. **GFC-6 Grade A ACCEPT**
rem2 **`e08cf83c`** / pin **`13bc0f48`** (`53aa2d6a` / rem
**`b79a3c0a`** are **B+** history): GET-polls the child review and
displays it at `needs_decision` without replacing a newer
continuation or the active primary. GET never dispatches.

**Completed this closeout:** **GFC-9 Grade A ACCEPT** rem4
**`899639f5`**. Implementation pin **`e53ce3b4`** is not acceptance.
Original **`d721ab4b`** / **`9ddc9900`** / rem **`1c9f926f`** / rem2
**`2d315bd5`** remain **Grade D** history; rem3 **`56e9781f`** remains
**Grade B+** history. Interleaved rec+rev slices, public
`building_review_materials`, sibling `review_window_ready`, API
**1.54.0**. Django still does not import `mappings_2`. Prior
**GFC-8 Grade A ACCEPT** **`911c11cd`** / pin **`bd73d0f9`**. Prior
**GFC-7B Grade A− ACCEPT** rem **`9b3e2424`** / pin **`7929eed7`**.
Policy **5**.

**Track next:** none (closed / archived). Product-default remains **FE-CM-4**.

---

## CRM duplicate review surfacing (RSF) (completed / archived)

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_duplicate_review_surfacing_and_field_fill_efficiency.md`.

**Does not displace product-default next FE-CM-4.** **RSF-1…RSF-5 Grade A
ACCEPT.** **RSF-5** **`96caece2`**. Django opens review when
`review_window_ready` even if auto-merge is pending. Auto-queued wait is
count-free. Django still does not import `mappings_2`.

**Track next:** **none** (closed / archived). See
`mappings_2/codex_context/open_work.md`.

---

## Completed — reconnect-in-place + direct file load (Grade A− ACCEPT; archived)

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_reconnect_in_place_and_direct_file_load.md`.

**Does not displace product-default next FE-CM-4.** Phases **1–3 Grade
A− ACCEPT.** Phase **3** rem3 **`f3307af5`** / pin **`ace3c352`**.
Lineage **`d185b833` + `09073680` + `d8496f35` + `f3307af5`**. Isolated
upload POST journals plan create from `dataset` / `raw_list` when ready.
Adoption requires a validated retry token bound to the current setup
revision. GET mutation status does not write the mapping draft. Phase
**2 Grade A− ACCEPT** rem **`06cc6c23`** / pin **`67589593`**. Phase
**1 Grade A− ACCEPT** rem **`84ee665f`** / pin **`3b05d8d0`**. Track
next: **none** (closed / archived).

---

## Completed — CRM disconnected-connection removal (Grade A- ACCEPT; mappings_2 authority)

**Phase 1 rem6 tip `11fc6f90`** Grade **A- ACCEPT**; rem7 nicety
**`885c3a87`**. Connect hub **Remove** next to Reconnect. POST journals
`crm_connection_delete` (`DELETE /v1/crm/connections/{id}`, HTTP **204**).
Django checks mutation state before any success banner. API **1.33.0**.
Does **not** displace **FE-CM-4**.

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_connection_disconnected_record_removal.md`.

---

## Completed — status-aware Remove dependents (Grade A ACCEPT; mappings_2 authority)

**Phases 0–6 Grade A ACCEPT.** Phase **6** code **`fad0d2d6`**, pin
**`3dd916a5`**. Only in-progress dependents block Remove. A rejected
Remove names the dependent and carries Technical details across
POST/redirect/GET. Archived. Does **not** displace **FE-CM-4**.

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_connection_removal_status_aware_dependents.md`.

---

## Completed / archived — CRM connection reclaim (2026-08-13)

**Phase 2 rem tip `b707ec4a`** (code **`de126af7`**) shipped Reclaim,
then same-day `ddd9a3b0` (2-OAUTH) regressed handle delivery on async
complete. Hardened Option A repaired it: Phase **0** **`ff15af8e`**
Grade **A− ACCEPT**; Phase **1** **`ffaa2d61`**, rem **`e5987926`**
Grade **A− ACCEPT**. First claimant-visible handle is the live 422 or
exactly one owner-authenticated `GET /v1/mutations/{key}`. Do not
rewrite this as the pre-regression “Closed” entry.

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_connection_reclaim_async_dispatch_regression.md`.
Parent:
`mappings_2/codex_context/cross_agent_eval/project_implementations/crm_connection_session_ownership_honesty.md`.
Does **not** displace **FE-CM-4**.

---

## Completed — CRM-dupe review clarity (DRC-1…4 closed / archived)

Normative strategy:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_duplicate_review_clarity.md`.

**DRC-0 rem2 Grade A- ACCEPT** tip **`3216235f`**. **DRC-1 Grade A-
ACCEPT** tip **`cce61414`**. **DRC-2 Grade A- ACCEPT** tip **`4820fa47`**.
**DRC-3 Grade A- ACCEPT** tip **`e0bf4529`** (docs pin **`ee15f077`**).
**DRC-4** pillars closeout. Track next: **none**. Does **not** displace
**FE-CM-4**.

---

## Adopted parallel (mappings_2 authority) — production HubSpot CRM duplicate path reliability

Normative strategy:
`mappings_2/.../production_hubspot_crm_duplicate_operator_path_reliability.md`.

**Completed:** Phase **0** `7edab8d2`; **1A Grade A- ACCEPT** tip **`74113c85`**
(docs pin **`84f1a6ad`**); **1B Grade A- ACCEPT** tip **`3a2ec943`** (docs pin
**`886584ca`**); **2+3 Grade A- ACCEPT** rem tip **`ce914f0a`** (docs pin
**`78d2ba3f`**). Launcher owns interpreter + startup diagnostics; API journey
store isolates unrelated hard-cut history; Django preserves base product
identity and active primary across review/continuation.

**Track next when authorized (not product-default):** **Phase 4A** network-free
full-path dual-process regression; then **4B** controlled production canary
(explicit per-run auth only). Storage E2E **7B** is not the production canary.
Does **not** displace **FE-CM-4**. No production write except explicit **4B**.

---

## Completed — Local storage E2E 7B/7C live FE (Grade A- ACCEPT; mappings_2 authority)

**7B tip `8ed8da6b`** (docs pin **`11113ac6`**): HubSpot Company live dual-process
FE (**non-production** live harness). **7C tip `bfad6a9d`** (docs pin
**`c2c62d7c`**): Salesforce Account live dual-process FE
(`test_local_crm_dupe_e2e_storage_phase7c`; refresh-token API launcher).
Network-free **1A–7A** retained. Track next **Phase 8** or **7D** (explicit SF
Person execute). Does **not** displace **FE-CM-4**.

Authority:
`mappings_2/.../local_crm_dupe_e2e_storage_and_operator_path.md`.

---

## Completed — Local storage E2E 2A/2B (Grade A- ACCEPT; mappings_2 authority)

**Tip `fd4692ef`** (land **`b4828438`**; docs pin **`d905fc46`**). Django:
registration Start over (lease-safe; inventory + connectability reconcile);
Technical details route/HTTP/`error_id` on Settings, Connect hub, setup wizards,
CRM-dupe start. Tests: `importer.test_registration_start_over_phase2a` (**17
passed**). Does **not** close storage E2E Phase **3** or **FE-CM-4**.

Authority:
`mappings_2/.../local_crm_dupe_e2e_storage_and_operator_path.md`.

---

## Completed — MAP-R1 Django catalog routing (Grade A; mappings_2 authority)

**MAP-R1 Grade A** tip **`89ff663c`** (land `5d85ece2`; pin `62485eea`).
`column_mapping_views` resolves Account/Contact/Lead/People-matching catalogs
from setup and **fails closed** on incomplete/unfrozen context or CRM
destination resolution errors (`mapping_blocked`; recovery to product setup /
files; no silent catalog invent). Tests:
`importer.test_column_mapping_map2` matrix coverage.

---

## Completed — MAP-2 column-mapping preflight UI (Grade A; mappings_2 authority)

**MAP-0–3 Grade A** (MAP-2 rem7 **`1f539885`**; pin **`a8640281`**; MAP-3 on
mappings_2). Web pieces: `web/importer/column_mapping_views.py`, templates,
session URLs under `/sessions/<id>/column-mapping/`, journaled API client
methods. Table + searchable picker **MAP-R4** Grade A. Backend matcher
**MAP-R2** Grade A; samples + atomic review **MAP-R3** Grade A tip
**`0e949cc7`**; linear happy path **MAP-R5 Grade A-** tip **`3190f7fe`**; track
next **MAP-R6**.

---

## Completed — CRM duplicate journey operator UX (no open findings)

**Docs tip `89eadea2`** (code tip **`89dd01da`**; land **`25b0ffae`**; rem
**`e0351944`**; CSS hygiene **`89dd01da`**). Makes the existing CRM duplicate
journey usable for operators without changing the merge dispatch boundary.

| Commit | Scope |
|---|---|
| **`25b0ffae`** | Progressive source-mode form; find-first default; group progress + winner confirm; merge-path labels |
| **`e0351944`** | Effect-label test updates; ignore/disable stale mode fields; selection highlight sync |
| **`89dd01da`** | Restore LF-only CSS additions so cumulative `git diff --check` is clean |

Evidence:

```text
python web/manage.py test importer
# Found 307 test(s). OK
python web/manage.py check  # clean
git diff --check 25b0ffae^..89dd01da -- web/importer  # clean
```

Does **not** close product-parallel **R5** dry_run E2E, live CRM execute, or
multi-group overview API projection. Merge path remains journaled
`effect-authorizations` (FE-EXEC tip **`c9d4d017`**).

---

## Completed — CRM setup UX 5-DL / 7A/7B / 5-SETUP (accepted Grade A)

| Slice | Accept tip | Scope |
|---|---|---|
| **5-DL** | **`7ef10ab6`** | Dual-process always-download package browser accept |
| **7A/7B** | **`bc09c7c8`** | Match-first defaults, D7/D8 picklist, hide destination (D6) |
| **5-SETUP** | **`5c6e11db`** | Dual-process browser accept for setup defaults |

Authority: `mappings_2/.../crm_connection_setup_ux.md`. Does **not** close
**HTTPS-2…3** or **FE-CM-4**.

---

## Completed — 0A-J journaled CRM app registration (accepted Grade A)

**Accept tip `34df1499`** (base `cc0d55c5`; rems `80757f22` / `2fcba793`). API
`Idempotency-Key` journal on registration routes; Django Settings form tokens +
pre-mutation secret validation. Does **not** close Connect UX Phase **1**.

---

## Completed — 7A-P local Settings (accepted)

**Tip `72746bc9`** (base `6936c61d`; rem1–2 `84eb35f2`/`2a444065`). Django
`/settings/` + API `/v1/settings/*`; loopback-only. Does **not** close R5.

---

## Completed — 7B-P HubSpot Settings registration (accepted Grade A)

**Accept tip `54c41083`** (pin `0c09e3d9`; rem2 `6d537438`; rem1 `1711a976`; land
`d3f1b7de` / land pin `b54ddd43`). HubSpot private-app / OAuth registration on
the same Settings shell; rotate + remove; restart-safe hub freeze
(cross-platform `file_insecure` test vault); API **1.10.0**. Focused Django
tests: `LocalSettingsHubSpotTests`. Does **not** authorize live HS query
(**7B-Q**) or displace R5.

---

## Completed — FE-EXEC (accepted)

**Accepted** tip **`c9d4d017`** (base `a1f9d28a`; rem1–2 `84ecc7a1`/`5ae405dd`;
pin `c2131546`; cleanup `e727142b`). Network-free browser → Django → API
**execute** on fake stack; does **not** broaden R5 dry_run. Details:
`mappings_2/.../crm_query_export_schema_and_campaign.md`.

| Gate | Result |
|---|---|
| FE-EXEC API + catalog | 10 passed |
| Focused real-process/UI | 9 passed |
| Full importer | 153 passed |

---

## Completed — intent wizard (accepted)

**Accepted** through tip **`f595497b`** (docs closeout follows).

| Phase | Commit | Scope |
|---|---|---|
| W1 | `fbe39788` | Pure router + intent UI + draft fields |
| W2 | `48bb9569` | Gate connected CRM; detach; freeze recovery; revision |
| W3 | `5e348203` | Detach generation; preclaim; product-key-only POST; fail-closed key |
| W4 | `f595497b` | Lock order; pending UI; replacement_of; revision parse |
| W5 | `2495fdab` | Pillar pins (codex_end); tip may be SHA-pin follow-up |

Operators choose Accounts/People and clean-only vs CRM matching; Django derives
existing product boxes. Backend catalog not unified. Network-free:
`python web/manage.py test importer` → **134 passed**.

### Disposition of prior priorities

| Prior item | Classification | Where context lives |
|---|---|---|
| Intent wizard implementation | **Completed** | this section + handoff phase table |
| Salesforce DR frontend lifecycle (was handoff “next”) | **Deferred** | below + lifecycle doc |
| Step 14 live dry-run | **Deferred** | below (full contract) |
| Connected-CRM import create bind | **Deferred** | below |
| Parent CRM R5 | **Unchanged** product parallel | `mappings_2/codex_context/open_work.md` |

---

## Completed — FE-CM-2 API client pin (accepted Grade A-)

**Accept tip `cb69bd39`** (land `61957c2c`; docs pin `962cd22e`). Django
generated client **1.12.0** / OpenAPI `70fbd546…` with
`CampaignMemberPolicyResource` + `ListImportCreate.campaign_member_policy`.
No configure UI in this slice (FE-CM-3).

---

## Completed — FE-CM-3 Django configure UI (accepted Grade A-)

**Accept tip `eea8e40f`** (land `a83566ea`; rem1 `fa7ebd0e`; docs pin
`edcb4afa`). List-import CampaignMember configure UI.

- Stack ceiling projection (not hard-disabled for SF/fake)
- Column maps + defaults + durable resolutions on create_workflow journal
- Campaign search/pick proxy; default mode **disabled** (ignores draft picks)
- Clear-draft POST action; drafts cleared after successful create
- Technical details show resolved Ids
- Suite: `importer.test_campaign_member_fe_cm3` (**28** tests)

## Completed — CRM-dupe first-batch hang (Grade A ACCEPT; mappings_2 authority)

**Phase 3 Grade A ACCEPT** code **`53846172`**, pin **`039d9c07`**.
First-batch implied-RA and OAuth complete are non-blocking Prefer/202;
package/export Prefer is no longer inert; dual-process barrier proofs
landed. Archived. Does **not** displace **FE-CM-4**.

Authority:
`mappings_2/codex_context/cross_agent_eval/project_implementations/completed_projects/crm_duplicate_first_batch_synchronous_authorization_hang.md`.

---

## Next recommended tasks (by track)

Product authority lives in `mappings_2/codex_context/open_work.md`.

| Track | Next | Notes |
|---|---|---|
| **Mapping product readiness** | **MAP-R6** (provider inventories) when authorized | MAP-R5 Grade **A-** tip **`3190f7fe`** complete |
| **List-import operator output** | **OUT-2** (this-track, session-authorized) | **OUT-H** Grade **A- ACCEPT** **`9fc3fa71`**. Does **not** displace **FE-CM-4**. |
| **ARW review-window integrity** | **closed / archived** | **ARW-5 Grade A ACCEPT** **`20d502b4`**; does **not** displace **FE-CM-4** |
| **CampaignMember** | **FE-CM-4** **product-default** | Web-heavy; not displaced by this archive |
| **Status-aware Remove** | **closed / archived** | Phases **0–6 Grade A ACCEPT**; Phase **6** **`fad0d2d6`**; does **not** displace **FE-CM-4** |
| **CRM-dupe abandon + Remove cascade** | **closed / archived** | Phases **0–4 accepted**; Phase **4 Grade A ACCEPT** **`d0974004`** / pin **`d72bedc9`**. Authority: `mappings_2/.../completed_projects/crm_duplicate_journey_abandon_and_connection_removal_cascade.md`. Does **not** displace **FE-CM-4** or **OUT-9** |
| **CRM-dupe journey UX** | **closed** (DRUX-1…5; DRC-1…4) | **DRC-3 Grade A- ACCEPT** **`e0bf4529`**; **DRUX-4 Grade A- ACCEPT** **`317e9d16`**; mappings_2 authority; does **not** displace **FE-CM-4** |
| **CRM-dupe first-batch hang** | **closed / archived** | Phase **3 Grade A ACCEPT** **`53846172`**; does **not** displace **FE-CM-4** |
| **BHR batch hydration** | **closed / archived** | **BHR-0…2 Grade A− ACCEPT** rem **`c7c910d2`**; backend-only; does **not** displace **FE-CM-4** |

### FE-CM-4 — Network-free browser → Django → API CM E2E (available parallel)

Authority: `mappings_2/.../crm_query_export_schema_and_campaign.md` Phase
**FE-CM-4**.

**Expected behavior:** End-to-end on fake stack: configure (maps + pick) → freeze
policy → dry_run (zero CRM mutation) → execute → receipts/technical details.
Negatives: replay/idempotency; stale plan; disconnect; ambiguous Campaign;
conflicting rows; HubSpot reject; malformed Id; blank without default.

```text
python web/manage.py test importer.test_campaign_member_fe_cm3
python web/manage.py test importer -k campaign
# FE-CM-4: full browser→Django→API CM journey on fake stack (when authored)
```

### Displaced web next (preserved)

| Prior web next | Classification | Context |
|---|---|---|
| **FE-CM-3** Django CM configure UI | **Completed** Grade **A-** tip **`eea8e40f`** | Suite 28; rem1 multi-pick/source/lookup-bound; rem2 disabled+clear |
| Browser-level visual QA of the intent wizard | **Deferred** (not obsolete) | Walkthrough fake-preview: Accounts/People clean vs match; detach on setup change; product keys only under Technical details. Resume as parallel QA when FE-CM not active. |

---

## Deferred visual QA — intent wizard (preserved)

Operator walkthrough against fake-preview (no implementation unless defects):

1. Accounts → clean only → upload → configure → start
2. People → clean only → Contacts and Leads
3. Accounts → match → list + CRM Accounts export → configure
4. People → match → list + Contacts/Leads → configure
5. Change setup after upload → detach incompatible → re-upload same role
6. Product keys only under Technical details

(Previously listed as web next; deferred so FE-CM-3 can be product-next.)

### Example

```text
Start an import
-> Accounts / Clean and prepare my file / Preview files only
-> Upload file -> Review settings -> Start import
-> Terminal or running workflow; technical details show frozen product_key
```

### Expected tests after any bugfix

```text
python web/manage.py test importer
# 134+ passed; no live CRM
```

---

## Deferred — Salesforce DR frontend lifecycle (post-FE-EXEC / post-UX)

**Not next.** Full operational context preserved (was prior handoff next task).
Web product next remains **FE-CM-4**.

Authoritative scope:
`mappings_2/codex_context/DUPLICATE_RESOLUTION_FRONTEND_LIFECYCLE.md`

**Completed subset (do not re-propose):**

- Network-free frontend **execute** E2E on the fake stack — **FE-EXEC** accept
  tip **`c9d4d017`** (partial / `paused_unknown` / exact-retry included)
- Operator UX usability for the CRM journey start + sequential winner confirm +
  merge-path labeling — tip **`89dd01da`** (stack above). Progressive
  find/upload mode, group progress, default-winner confirm, effect-auth labels,
  and cumulative CSS `--check` hygiene.

**Still deferred (context preserved):**

- Product-parallel **R5** dry_run E2E through full frontend journey (both source
  modes) — product-parallel in mappings_2 `open_work.md` (not web FE-CM next)
- Live CRM execute / provider live gates (parent Phase 5; strategy 7C–7E)
- Multi-group overview of all discovered groups before sequential review
  (would need API projection of group summaries; not present today)
- Optional acquire_all-first E2E test beyond candidate_upload FE-EXEC drive
- Operator polish beyond fake-stack FE-EXEC (leakage at live OAuth, SF/HS
  register-only stacks when cheap under R5)

Expected behavior for remaining lifecycle work:

- Connect CRM; API-owned auth; no tokens in Django/browser
- Contacts or Accounts; upload candidates or complete acquisition
- Explicit review of every group; authorize frozen plan; guarded merges; receipts
- Connection / recommendation never imply execute

Example (R5 dry_run ceiling — product-parallel, not web FE-CM next):

```text
Duplicate resolution -> Connect fake CRM -> Accounts
-> Find or upload candidates -> review every group
-> authorize dry_run -> terminal (zero execute; zero live CRM)
```

Expected tests: R0–R4 still green; new R5 E2E when authored; OAuth
replay/leakage; both source branches; explicit decisions; stale/wrong-target.
Live gates separately authorized.

Note: home page already has CRM connect + duplicate journey paths. FE-EXEC
proves execute on fake stack; UX tip **`89dd01da`** makes the start/review/merge
labels usable; R5 remains the dry_run product bar.

---

## Deferred — Connected CRM on import matching

**Mostly completed by strategy Phase 5** (`cdff89c5`): list/account-list create
binds `connection_id` + RA execute; wizard connection pick enabled. Residual UX
polish only — not a greenfield gap. Do not re-open as “not built.”

---

## Deferred live acceptance gate — Step 14 Account-list dry-run

**Deferred** by prior explicit user direction (not completed or obsolete).

Network-free prerequisite: `1d7d489 Complete Step 14 verification boundary`.

Expected behavior:

- API `127.0.0.1:8000` alone resolves `dev`, owns OAuth credentials and live
  capabilities, and re-verifies alias identity, OAuth identity, the exact
  expected org, `IsSandbox = false`, and `Developer Edition` on every
  capability method.
- Django `127.0.0.1:8001` sees only the opaque configured provider and public
  workflow contract. No credential, token, customer row, plan, ledger, log, or
  raw Salesforce identifier crosses the browser boundary or enters AI-visible
  output.
- Every API and Django state, credential, ledger, artifact, evidence, log,
  database, and media path resolves outside the repository. Django rejects
  `DATABASE_URL` in this profile.
- Operator submits two deliberately duplicate synthetic Account rows through
  the ordinary upload journal, restarts the API at `paused_verification`,
  resumes the exact reference attempt, resolves exactly one `list_duplicates`
  checkpoint, and explicitly authorizes Account `dry_run` plus delivery
  `preview`.
- Workflow reaches terminal state with dry-run receipts and artifacts.
  Independent before/after verification proves zero Salesforce mutation and
  unchanged marked fixture state.

Provider composition, opaque binding, no-write transport, current-ceiling
enforcement, and private evidence remain as previously accepted for the
network-free Step 14 implementation. Live execution requires separate
explicit authorization.

---

## Historical completed notes (not living tasks)

- API v1.2.0 grouped-option decisions and presentation refinement `4807cd2a`
- Step 13 frontend; Step 14 network-free implementation

No Salesforce access is authorized by this backlog file alone.
