# EasyImports Web — Thin strategy

This area has no indexable scoping documents. The only living context
document is `current_state.md`.

## Required context order

1. `web/codex_context/current_state.md` — implemented Django/web behavior

Then read only the user-named product document relevant to the task, plus
root `codex.md` and `CONTEXT_PILLARS.md`.

Work starts from explicit operator text, a confirmation of a worker
proposal, or a scoped strategy the user names. This area has no queue
and no catalog. Absence of those files is the expected state.

## Historical context (optional)

Dated pre-cleanup snapshots live under
`web/codex_context/archived/context_pillars_pre_cleanup_2026-08-19/`.
They are non-normative. Do not read them at session start or use them to
choose a task.
